using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoveItem : MonoBehaviour
{
    public int bindRouteIndex = 0;
    // Start is called before the first frame update
    void Start()
    {
        
        if(bindRouteIndex != 0)
        {
            
        }
    }

    // Update is called once per frame
    void Update()
    {
        if(bindRouteIndex != 0)
        {
            
        }
    }
}
